# PROTOTYPE — 巡检工具交互

用于回答：脚本管理与报告生成两个独立模块应采用什么页面结构，才能清楚承载脚本获取、日志提交、多实例报告编辑和导出？

三个结构差异明显的方案位于同一页面，通过 `?variant=A`、`?variant=B`、`?variant=C` 或底部切换器比较。

## 运行

```bash
./run.sh
```

然后访问 <http://localhost:4173>。

这是无后端、无持久化的 throwaway 原型。刷新页面会重置所有状态。
